import React from "react";
import ProjectCards from "../Card";

const Portfolio = () => (
  <div>
    <h1>Portfolio</h1>
    <div>
      <ProjectCards />
    </div>
  </div>
);

export default Portfolio;
